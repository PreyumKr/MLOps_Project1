[build]
  command = "pip install -r requirements.txt"
  functions = "netlify/functions"
  publish = "."

[functions]
  directory = "netlify/functions"
  included_files = ["main.py", "src/**/*.py", "requirements.txt"]
  node_bundler = "none"